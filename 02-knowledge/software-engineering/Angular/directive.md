---
tags:
  - area/knowledge
  - area/software-engineering
  - topic/angular
  - topic/docker
---

# Directive

**ng-container** directive 
- avoid having to create that extra div, we can instead use ng-container directive
- can also provide a **placeholder for injecting a template dynamically into the page**.

Dynamic Template Creation with the **ngTemplateOutlet** directive
 

Refering to the loading template via its template reference #loading,
and we are using the ngTemplateOutlet structural directive to instantiate the template.

input variable is called **lessonsCounter**, and it's defined via a ng-template property using the prefix **let-**

 
 **We can pass a template as an input parameter**

enum Role {Employee, Manager, Admin};

 HostBinding decoration is new - it lets us set attributes on the host element. The host is the
element this component is attached to

## Related
- [[software-engineering-moc]]
- [[elasticsearch]]
